# Copy this file to .env.local (which git ignores) and fill in the values.
# Ask Kevin for them.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional: the captain password. When set, roster captains are unlocked
# automatically instead of being asked for it (still verified server-side).
VITE_CAPTAIN_PASS=
